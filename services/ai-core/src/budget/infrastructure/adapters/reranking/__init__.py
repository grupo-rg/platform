"""Reranking adapters — cross-encoders y similar.

Vive en infrastructure porque depende de modelos externos (ML); el port
(si fuese necesario) iría en `src/budget/application/ports/`. Por ahora
solo hay un implementador y se inyecta directamente en SwarmPricingService.
"""
